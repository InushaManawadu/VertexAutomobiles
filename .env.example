VITE_RECAPTCHA_V3_SITE_KEY=your_recaptcha_v3_site_key

# Optional for local development only. Use "true" to print a debug token in the
# browser console, then register that token in Firebase App Check debug tokens.
# Never commit a real debug token.
# VITE_FIREBASE_APPCHECK_DEBUG_TOKEN=true
